
<ul class="breadcrumb">
  <li><a href="/">Home</a> <span class="divider">/</span></li>
  <li class="active"><%= @technology_type.name %></li>
</ul>


<div class="container-fluid">

    <h1>Find out what people think of:</h1>
    <ul class="unstyled">
        <% cache ['technology-index', @technology_type, @technologies] do %>
            <% @technologies.each do |technology| %>
                <% cache ['technology-index', @technology_type, technology] do %>
                    <li class="row-fluid">
                        <h2><%= link_to technology[:name], technology_path(technology), class: "span12" %></h2>
                    </li>
                <% end %>
            <% end %>
        <% end %>
    </ul>

    <p>Something missing? <%= link_to "add new", new_technology_path(:technology_type_id => @technology_type.id), class: "btn" %></p>
</div>
