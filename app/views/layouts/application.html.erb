<!DOCTYPE html>
<html>
<head>
  <title>Technophobia</title>

  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
    <div class="navbar navbar-static-top">
        <!--navbar-fixed-top-->
        <div class="navbar-inner">
            <div class="container-fluid">

                <a class="brand" href="/">technophobia</a>

                <ul class="nav pull-right">
                    <li>
                        <form class="navbar-search" action="/search">
                            <input type="text" name="search_term" placeholder="search" class="search-query"></textfield>
                            <button type="submit" class="hide"></button>
                        </form>
                        <% form_for :search, url: search_index_path,
                                    html: {class: "navbar-search"}, method: "GET" do |f| %>
                            <%= f.text_field :search_term, placeholder: "search", class:"search-query"%>
                            <%= f.submit "search", :class=>"hide" %>
                        <% end %>
                    </li>
                    <% if user_signed_in? %>
                        <li><%= link_to 'Settings', edit_user_registration_url%></li>
                        <li class="divider-vertical"></li>
                        <li><%= link_to 'Sign out', destroy_user_session_url, :method => :delete %></li>
                    <% else %>
                        <li><%= link_to 'Sign in', new_user_session_url %></li>
                        <li class="divider-vertical"></li>
                        <li><%= link_to 'Sign up', new_user_registration_url %></li>
                    <% end %>
                </ul>
            </div>
        </div>
    </div>
    <% if alert %>
        <div class="alert alert-error">
            <button type="button" class="close" data-dismiss="alert">×</button>
            <%= alert %>
        </div>
    <% end %>
    <% if notice %>
        <div class="alert alert-info">
            <button type="button" class="close" data-dismiss="alert">×</button>
            <%= notice %>
        </div>
    <% end %>

<%= yield %>

</body>
</html>
