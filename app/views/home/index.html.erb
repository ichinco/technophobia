<%= form_for :search, url:technology_index_path,
    html: {class: "form-inline"}, method: "GET" do |f| %>
    What
    <%= f.text_field :technology_type, :data =>
            { :provide => 'typeahead',
              :source => @type},
            placeholder: "framework"%>
    should I use
    <%= f.submit "?", :class=>"btn" %>
<% end %>